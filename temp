构建企业级管理系统，以及处理复杂的数据流水线，对底层运行环境的隔离性和稳定性要求极高。既然这台海光 C86 物理机已经拥有了完美的网络底座，我们依然要贯彻“环境隔离、版本可控”的架构师原则，绝不能让各种语言的依赖包污染 UOS 的系统全局环境。

针对 Python 和 Java 的开发需求，为你推荐以下企业级标准工具栈及部署步骤：

### 一、 Python 开发栈：Miniconda (隔离与数据处理利器)

无论是对接 OpenModelica 进行液压机电仿真控制，还是通过 API 批量抓取和清洗 ETF 量化交易数据，Python 都会涉及大量底层 C/C++ 编译的科学计算库（如 NumPy、Pandas）。使用系统自带的 Python 极易引发依赖冲突。

**Miniconda** 是目前业界最成熟的 Python 虚拟环境管理器。

**1. 一键下载与静默安装**
在终端中执行（借助已生效的终端代理，拉取速度会非常快）：

```bash
mkdir -p ~/miniconda3
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda3/miniconda.sh
bash ~/miniconda3/miniconda.sh -b -u -p ~/miniconda3

```

**2. 初始化并注入环境变量**

```bash
~/miniconda3/bin/conda init bash
source ~/.bashrc

```

**3. 配置国内极致加速源 (清华镜像)**
为了后续 `conda install` 和 `pip install` 更加稳定快捷：

```bash
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/
conda config --set show_channel_urls yes
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

```

*完成后，你可以随时用 `conda create -n myenv python=3.10` 来为不同的业务模块创建纯净的沙盒。*

---

### 二、 Java 企业级开发栈：SDKMAN! (全能版本管家)

Java 企业管理系统（如 Spring Boot 架构）在开发、测试和压测阶段，经常需要切换不同版本的 JDK（如 JDK 8, 11, 17, 21）以及构建工具。**SDKMAN!** 就是 Java 界的 NVM。

**1. 安装 SDKMAN!**

```bash
curl -s "https://get.sdkman.io" | bash
source "$HOME/.sdkman/bin/sdkman-init.sh"

```

**2. 安装长期支持版 (LTS) JDK**
企业级开发目前主流推荐 JDK 17 或 21。这里我们安装 Eclipse Temurin（前身是 AdoptOpenJDK，业界公认最稳定的开源构建版）：

```bash
# 查看所有可用版本：sdk list java
# 直接安装 JDK 17 LTS
sdk install java 17.0.11-tem 

```

**3. 安装企业级构建工具**
一键补齐 Maven 和 Gradle：

```bash
sdk install maven
sdk install gradle

```

*至此，你的 Java 编译底座已经完美搭建。*

---

### 三、 核心基础设施：Docker (容器化引擎)

开发企业管理系统必定离不开数据库（PostgreSQL/MySQL）、缓存中间件（Redis）或消息队列。在物理机上直接装这些中间件是开发大忌，**全部容器化运行**才是正解。

**1. 安装 UOS 适配的 Docker 引擎**

```bash
sudo apt install docker.io docker-compose -y

```

**2. 启动并设置开机自启**

```bash
sudo systemctl enable --now docker

```

**3. 免 Sudo 运行 Docker (提升开发幸福感)**
默认情况下每次敲 docker 都要加 `sudo`，非常繁琐。把当前用户加入 docker 组：

```bash
sudo usermod -aG docker $USER

```

*注意：执行完此命令后，需要彻底注销当前 UOS 桌面系统并重新登录，或者执行 `newgrp docker` 才能生效。*

---

### 四、 IDE 联动与赋能

你刚才安装的 **Windsurf** 已经是一款顶级的 AI 双核驱动 IDE，它能完美接管上述所有环境：

* **对于 Python：** 在 Windsurf 中打开你的项目文件夹，按下 `Ctrl + Shift + P`，输入 `Python: Select Interpreter`，它会自动识别出你用 Conda 创建的各个虚拟环境，直接选中即可。
* **对于 Java：** 在 Windsurf 的扩展商店中，搜索并安装微软官方的 **Extension Pack for Java** 和 **Spring Boot Extension Pack**。它们会自动读取 SDKMAN! 配置的 JDK 和 Maven 路径。

现在，这台海光服务器不仅打通了国际网络脉络，其上层建筑——Node.js (Slidev 前端)、Python (仿真与量化中台)、Java (企业级后台) 以及 Docker (容器底座) 已经全部按照最高工业标准就绪。随时可以建立项目工程了！
