codeium.windsurfpyright
danielroedl.meld-diff
foam.foam-vscode
ms-python.debugpy
ms-python.python
ms-python.vscode-python-envs
rooveterinaryinc.roo-cline


既然你要在 Windsurf 里依靠 **Roo Code (原 Roo Cline)** 扩展来灵活外接其他大模型（比如通过 API 连接 DeepSeek、Claude 等），那么确保 Windsurf 的插件能够完美吃到你的 v2rayA 代理，就是维持高响应率的生命线。

以下是针对这两个需求的架构师级快速指南：

---

## 一、 Windsurf 怎么设置使用 v2rayA 代理？

虽然我们在系统里设置了 `~/.bashrc` 的终端代理，但由于 UOS 的桌面环境隔离，通过图标启动的 Windsurf 可能会漏掉环境变量。为了给 Roo Code 的 API 请求拉起最稳妥的“防丢包”隧道，我们需要在 Windsurf 内部进行显式对齐。

### 1. 软件内强制绑定 HTTP 代理

1. 在 UOS 的 Windsurf 界面中，按下键盘快捷键 `Ctrl + ,`（打开设置）。
2. 在顶部的搜索栏输入：**`proxy`**。
3. 找到 **`Http: Proxy`**，在输入框中填入你 v2rayA 的本地出口：
```text
http://127.0.0.1:20171

```


4. 找到 **`Http: Proxy Strict SSL`**，**务必将其“取消勾选”**（关闭严格 SSL 校验，防止信创环境本地证书冲突导致 Roo Code 连接 API 时报错）。

### 2. 检查 Roo Code 的连接

在 Windsurf 内安装好 Roo Code 扩展后，你在配置 API Key（如 OpenRouter、Anthropic 或 DeepSeek）时，由于有了上述设置，Roo Code 发起的所有底层网络请求都会被无缝重定向到 v2rayA 隧道中。

---

## 二、 快速将 Windows 上的插件移植到 UOS（拒绝手动一个个搜）

千万不要在 UOS 的应用市场里一条条去搜索重新下载，VS Code 生态（包括 Windsurf）有非常高效的一键克隆方法。我们可以通过**一条终端命令**，直接把 Windows 的插件列表导出，再到 UOS 里一键“复刻”。

因为 Windsurf 的命令行叫 `windsurf`（或者你刚刚注册的快捷命令 `serf`），它的参数与 VS Code 完全兼容。

### 第一步：在 Windows 电脑上导出插件列表

1. 在 Windows 上打开终端（PowerShell 或 CMD）。
2. 执行以下命令，将当前安装的所有插件名字导出为一个文本文件：
```powershell
windsurf --list-extensions > extensions.txt

```


*(如果提示找不到 windsurf 命令，说明你在 Windows 上装的是 VS Code，那就可以把命令改成 `code --list-extensions > extensions.txt`)*

### 第二步：把 `extensions.txt` 传到 UOS

用那根 `192.168.100.x` 的网络连线或者 ToDesk，把这个 `extensions.txt` 文件直接传到 UOS 的 **家目录 (`~`)** 下。

### 第三步：在 UOS 上一键自动化安装

打开 UOS 的终端，利用 Linux 强大的管道机制，一键让系统自动把列表里的所有插件全部挂代理拉下来：

```bash
# 确保当前的终端已经加载了代理环境（如果没加载可以先敲 export 那两行）
cat ~/extensions.txt | xargs -n 1 windsurf --install-extension

```

系统会开始刷屏，自动去市场上把你 Windows 里的 Roo Code、以及你熟悉的所有主题、高亮、语言包无缝下载并安装到这台海光 C86 物理机上。

---

### 💡 架构师的最后提醒：配置文件（Settings.json）也可以迁移

如果你在 Windows 上的 Roo Code 里已经配好了大模型的 API 密钥（Keys）和自定义 Prompts，不想在 UOS 上重新配一遍，你可以直接去 Windows 的以下路径找到配置文件：

* `%APPDATA%\Windsurf\User\settings.json`

打开它，把里面关于 Roo Code 的配置片段（通常以 `"roo-cline.`" 或 `"cliner.`" 开头），直接复制粘贴到 UOS 版 Windsurf 的 `settings.json` 中。这样，你的整个 AI 开发手感就 100% 完美平移过来了！

现在可以执行导出和一键安装了，搞定后你的 UOS 编程堡垒就彻底满血复活了。

```

```
